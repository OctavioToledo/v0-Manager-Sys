# Base de datos — Supabase PostgreSQL
DB_URL=jdbc:postgresql://<host>.pooler.supabase.com:6543/postgres
DB_USERNAME=postgres.<project-ref>
DB_PASSWORD=

# Supabase Auth — JWT secret (Dashboard > Settings > API > JWT Secret)
SUPABASE_JWT_SECRET=

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5174,https://om-time.vercel.app
